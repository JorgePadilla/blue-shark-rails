%h1 Listing doctors

%table
  %thead
    %tr
      %th First name
      %th Second name
      %th Last name
      %th Second last name
      %th Mobile
      %th Email
      %th Specialty
      %th College number
      %th
      %th
      %th

  %tbody
    - @doctors.each do |doctor|
      %tr
        %td= doctor.first_name
        %td= doctor.second_name
        %td= doctor.last_name
        %td= doctor.second_last_name
        %td= doctor.mobile
        %td= doctor.email
        %td= doctor.specialty
        %td= doctor.college_number
        %td= link_to 'Show', doctor
        %td= link_to 'Edit', edit_doctor_path(doctor)
        %td= link_to 'Destroy', doctor, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Doctor', new_doctor_path
